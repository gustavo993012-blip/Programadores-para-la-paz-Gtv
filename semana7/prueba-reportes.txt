Al ejecutar la ruta GET del ejercicio, notamos que en esta ocasión no muestra un mensaje como servidor activo en ruta 3000, noto que me muestra un objeto con los elementos de contiene la variable/const reporte la cual es un objeto, pero también noto que me muestra corchetes al inicio y final, lo que quiere decir que este objeto se guarda en un array, dando estructura que queda como [Elemento1, Elemento2, Elemento3] y así con cada nuevo reporte ingresado, por supuesto con la estructura que contiene la const reporte, también podemos destacar que el primer elemento del objeto es elemento que incrementa con cada nuevo reporte agregado, dando a lugar un ID personalizado para cada elemento, haciendo su búsqueda mas eficiente. 
